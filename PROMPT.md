#Geração do docker compose + DockerFile

Com base na arquitetura alvo que será implementada crie um docker compose e Dockerfile, com
  os servicos que vou precisar: rabitmq para fila, redis para cache e postgresql para o banco de
  dados. e tbm um service no docker-compose para executar a aplicação, para q quem for executar, n dependa do
  node instalado na propria Maquina.